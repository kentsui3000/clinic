# 部署 Checklist — 徐嘉賢診所員工分紅自動化系統

從 .xlsx 模板到正式上線，**4 大步、預估 20 分鐘**（v1.2 一鍵建 Form）。

> 全程以徐醫師 Google 帳號登入，不要中途切換帳號。

---

## Step 1 — 上傳試算表（5 分鐘）

- [ ] 把 `clinic_bonus_template.xlsx` 拖到 Google Drive
- [ ] Drive 右鍵此檔 → **開啟方式 → Google 試算表**
- [ ] 檔案 → 重新命名為「**徐嘉賢診所分紅系統**」
- [ ] 確認 7 張分頁都在（說明 / 員工總表 / 單價參數 / Form回應_當班 / Form回應_代謝 / 分紅明細 / 月度結算）
- [ ] 「月度結算」A2 顯示 QUERY 結果或 `#N/A`（**不是** `#NAME?`）
- [ ] 「員工總表」5 位員工初始資料正確

---

## Step 2 — 安裝 Apps Script + 一鍵建 Form（5 分鐘）

### 2.1 安裝腳本

- [ ] 在 Sheets 上方 **擴充功能 → Apps Script**
- [ ] 編輯器預設 `Code.gs` 全選刪除
- [ ] 把 `apps_script.gs` 整份內容貼進去
- [ ] 左上專案名稱改為 `徐嘉賢診所分紅系統`
- [ ] 💾 儲存

### 2.2 一鍵建立兩張 Form

- [ ] 編輯器頂端「選擇要執行的函式」下拉選 **`createBothForms`** → 點 ▶ 執行
- [ ] 首次執行會跳授權對話框：**進階 → 前往「徐嘉賢診所分紅系統」(不安全) → 允許**
  > 「不安全」是 Google 對未驗證腳本的標準警告；自己的腳本沒問題
- [ ] 等待約 10 秒，下方執行記錄會印出：

  ```
  ===== Form 建立完成 =====

  【Form 1:每日當班結帳】
    填寫網址(給員工): https://docs.google.com/forms/d/e/.../viewform
    編輯網址(管理用): https://docs.google.com/forms/d/.../edit
    Form ID         : ...

  【Form 2:代謝症候群活動記錄】
    填寫網址(給員工): https://docs.google.com/forms/d/e/.../viewform
    編輯網址(管理用): https://docs.google.com/forms/d/.../edit
    Form ID         : ...
  ```

- [ ] **把這段執行記錄整段複製貼到記事本備用**（後面 Step 4 要用）
- [ ] 回 Sheets 確認多了兩張分頁：`Form回應_當班`、`Form回應_代謝`（原本的空白佔位分頁已自動被取代）

> ✅ Form 1 的 9 個欄位、Form 2 的 6 個欄位、所有選項、必填、數字驗證、安全設定（關閉編輯回應、關閉顯示其他回應、開啟進度列、自訂確認訊息）全部由腳本自動完成、字串對齊 Apps Script，**不需要手動建任何欄位**。

---

## Step 3 — 設觸發器 + 首次同步員工（3 分鐘）

### 3.1 設定 3 個觸發器（Apps Script 編輯器左側 ⏰ 圖示）

- [ ] **觸發器 1**：函式 `syncEmployeesToForms` / 來源「來自試算表」/ 類型「編輯時」
- [ ] **觸發器 2**：函式 `onDailyFormSubmit` / 來源「來自試算表」/ 類型「表單提交時」
- [ ] **觸發器 3**：函式 `onMetaFormSubmit` / 來源「來自試算表」/ 類型「表單提交時」

> Form 1、Form 2 兩個 onSubmit 觸發器都會在任何表單提交時被叫到，但 apps_script.gs v1.1+ 的守衛會確保各自只處理對應的 Form。

### 3.2 首次同步員工名單

- [ ] 編輯器頂端選函式 `syncEmployeesToForms` → ▶ 執行
- [ ] 執行記錄應出現：`員工名單同步完成,目前在職:芯儀、郁芹、佩芷、嘉惠、春言`

### 3.3 驗證 Form 內容

- [ ] 從 Step 2.2 複製的「Form 1 編輯網址」打開 Form 1，點右上眼睛預覽：
  - [ ] 「本時段值日生」下拉看到「無 / 芯儀 / 郁芹 / 佩芷 / 嘉惠 / 春言」
  - [ ] 「本班當值人員」核取方塊看到 5 位員工（沒有「(待同步)」字樣）
  - [ ] 「填寫人」、「核對人」下拉看到 5 位員工
- [ ] 「Form 2 編輯網址」打開預覽，「執行員工」下拉看到 5 位員工

---

## Step 4 — 試跑驗證 + 正式上線（7 分鐘）

### 4.1 試跑 7 個 case

從 Step 2.2 記下的 **Form 1 / Form 2 填寫網址** 提交以下測試資料，每次提交後到 Sheets「分紅明細」分頁檢查最後幾列。

#### Case A：基礎分紅 + 值日生（黃金路徑）

- [ ] Form 1 提交：今天 / 上午 / 值日生`芯儀` / 當班勾`芯儀+郁芹+嘉惠` / 看診`80` / 流感`10`
- [ ] **預期分紅明細 +4 列**：芯儀基礎100、郁芹基礎100、嘉惠基礎100、芯儀值日生100

#### Case B：晚上時段不發值日生津貼

- [ ] Form 1 提交：晚上 / 值日生`無` / 看診`80` / 流感`10` / 當班 3 人
- [ ] **預期**：3 列基礎分紅 100，**0 列值日生**

#### Case C：未達門檻不產生空紀錄

- [ ] Form 1 提交：上午 / 值日生`無` / 看診`50` / 流感`0`
- [ ] **預期**：分紅明細**完全沒有新列**

#### Case D：流感方案 Y 邊界

- [ ] Form 1 提交：上午 / 值日生`無` / 看診`50` / 流感`10` / 當班 2 人
- [ ] **預期**：2 列基礎分紅 50（公式：MAX(0, 50-10-60)×5 + 10×5 = 50）

#### Case E：代謝-收案

- [ ] Form 2 提交：今天 / 上午 / `收案` / `佩芷` / 患者代號`001`
- [ ] **預期**：1 列 — 佩芷 / 代謝-收案 / 100

#### Case F：代謝-追蹤

- [ ] Form 2 提交：今天 / 下午 / `追蹤` / `郁芹` / 患者代號`002`
- [ ] **預期**：1 列 — 郁芹 / 代謝-追蹤 / 20

#### Case G：計算單元測試

- [ ] Apps Script 編輯器選函式 `testCalculation` → ▶ 執行
- [ ] 執行記錄 6 行全部 `✓`

#### Case H：月度結算 QUERY

- [ ] 開「月度結算」分頁
- [ ] A2 顯示本月各員工總分紅（依測試 case 累積值）
- [ ] D2、I2、L2 都正常運作

### 4.2 清空測試資料

- [ ] 「分紅明細」第 2 列起全選 → 刪除（**保留標題列**）
- [ ] 「Form回應_當班」第 2 列起全選 → 刪除
- [ ] 「Form回應_代謝」第 2 列起全選 → 刪除

### 4.3 權限與分享

- [ ] Sheets 右上「共用」確認：
  - [ ] 一般存取權「**僅限有權限的使用者**」
  - [ ] 共用對象僅有徐醫師（擁有者）+ 主任（編輯者，選填）
  - [ ] **絕對不要**把 Sheets 連結給任何員工
- [ ] 兩張 Form 確認：
  - [ ] 預設「**任何擁有連結的使用者都可以填寫**」（不需登入）

### 4.4 把 Form 連結發給員工

從 Step 2.2 記下的「**填寫網址**」（不是編輯網址）：

- [ ] 用任何短網址服務（bit.ly、PicSee、reurl.cc 等）縮短
- [ ] 或直接用 Google Form 「傳送」按鈕生成 QR Code
- [ ] 印貼紙貼在櫃台 / 放診所內部 LINE 群
- [ ] **不要分享編輯網址**（編輯網址會讓員工改 Form 結構）

### 4.5 教育員工 3 分鐘

- [ ] **晚上時段值日生請選「無」**
- [ ] 本班當值人員「**全部勾選該時段在場員工（含值日生本人）**」
- [ ] 流感支數欄位「**沒有填 0**，不要留空」
- [ ] 結帳時段提交：上午下診後、下午下診後、晚上下診後各填一張
- [ ] 看到「已收到，謝謝!」即成功；不要重覆送出
- [ ] 看到錯誤 → 截圖傳主任，**不要自行修改任何東西**

---

## 完成 Checklist

- [ ] Step 1 — Sheets 上傳並轉檔
- [ ] Step 2 — Apps Script 安裝 + `createBothForms()` 執行成功
- [ ] Step 3 — 三個觸發器設定 + `syncEmployeesToForms()` 首次同步
- [ ] Step 4 — 試跑 8 個 case + 清空測試資料 + 權限設定 + Form 連結發出

🎉 全部勾完即正式上線。

---

## 重新建立 Form 的方法

如果建錯了想砍掉重練：

1. Google Drive 找到「每日當班結帳」「代謝症候群活動記錄」兩張 Form，移到垃圾桶
2. Sheets 把「Form回應_當班」「Form回應_代謝」分頁刪除
3. Apps Script 編輯器 → **專案設定 ⚙ → 指令碼屬性 → 刪除** `FORM_ID_DAILY` 與 `FORM_ID_META`
4. 重新執行 `createBothForms()`

---

## 常見問題排查

| 症狀 | 可能原因 | 處理 |
|---|---|---|
| 月度結算顯示 `#NAME?` | 還是 .xlsx 沒轉成 Google Sheets | 重新上傳 + 右鍵以 Sheets 開啟 |
| 月度結算顯示 `#REF!` | 分頁名被改 | 改回 `分紅明細` |
| `createBothForms()` 跳「Form 已建立過」 | 重複執行 | 照上方「重新建立 Form 的方法」步驟 |
| 員工同步後 Form 下拉沒變 | Form 預覽分頁有快取 | 重新整理 Form 預覽分頁 |
| Form 提交後分紅明細沒新增 | 觸發器未設定 / 員工填錯數字 | 查 Apps Script 執行記錄 |
| Logger 看到「找不到分紅明細工作表」 | 分頁被改名 | 改回 `分紅明細` |
| Form 1 提交 `onMetaFormSubmit` 沒輸出 | **正常** — 守衛擋掉了 | 不用處理 |
| 忘記 Form 填寫網址 | — | 執行器執行 `printFormUrls()` 重新印出 |

---

**版本**：v2.0（一鍵建 Form 流程）
**對應 apps_script.gs**：v1.2
**最後更新**：2026-05-13
